# PostgreSQL connection used by Prisma. Phase 1 validates configuration but does
# not connect to or migrate a database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/world_cup_simulation?schema=public"

# Public application origin used for metadata and future authenticated saves.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
